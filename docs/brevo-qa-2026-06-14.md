## Brevo campaign readiness QA - 2026-06-14

Test window: 2026-06-14, around 20:59-21:05 UTC.

## Test contacts

| Flow | Test email | Result |
| --- | --- | --- |
| Contact form workflow | `kenny+brevoqa-contact-202606142059@alphatrack.digital` | Passed Brevo list/workflow test |
| Tracking audit workflow | `kenny+brevoqa-audit-202606142059@alphatrack.digital` | Passed Brevo list/workflow test; later added to suppression list #14 |
| Newsletter workflow | `kenny+brevoqa-newsletter-202606142059@alphatrack.digital` | Passed Brevo list/workflow test |
| Exit popup workflow | `kenny+brevoqa-exit-202606142059@alphatrack.digital` | Passed Brevo list/workflow test |
| Strategy call workflow | `kenny+brevoqa-strategy-202606142059@alphatrack.digital` | Passed Brevo list/workflow test |
| Blocklist test | `kenny+brevoqa-blocked-202606142059@alphatrack.digital` | Blocklist flag present, but workflow log still says email was sent |
| Deployed backend contact test | `kenny+brevoqa-backend-contact-202606142059@alphatrack.digital` | Backend accepted and created contact, but deployed backend lacks latest CRM/campaign metadata fallback |
| Frontend contact-page test | `kenny+brevoqa-frontend-contact-202606142059@alphatrack.digital` | Frontend created contact after footer field was filled, but payload lacks latest campaign metadata |

## Passed

- Brevo Automations list shows five active workflows and no inactive/redundant Automation #3.
- Workflow #4 Tracking Audit Nurture started for list #11 and sent the first email.
- Workflow #5 Strategy Call Workflow started for list #7 and sent the first email.
- Workflow #6 General Enquiry Workflow started for list #8, sent the first email, and entered the 2-day wait.
- Workflow #7 Newsletter Workflow started for list #9, sent the welcome email, and exited.
- Workflow #8 Exit Popup Workflow started for list #10, sent the template #30 email, and exited.
- Direct Brevo contact checks confirmed expected list IDs and campaign attributes for the API-created test contacts.
- Deployed backend API at `https://alphatra-serv.netlify.app/api/leads` accepted a contact-form payload from the test origin.

## Failed or incomplete

- CRM deals/tasks remained at zero during QA because the deployed `alphatra-serv` backend branch does not yet include commit `03470e5` with the CRM API fallback.
- The deployed backend contact test wrote only the older attributes (`FIRSTNAME`, `LASTNAME`, `MESSAGE`, `SOURCE`, etc.) and did not write `LEAD_SOURCE`, `WEBSITE_ROUTE`, `OFFER`, `CONSENT_STATUS`, or `CONSENT_TIMESTAMP`.
- The frontend test page posted to `https://alphatrackdigital.netlify.app/api/leads`; it created a contact, but with the older payload shape.
- The contact-form submit was blocked until the footer newsletter email field was filled, which suggests page-level validation can interfere with the contact form.
- Adding the audit test contact to suppression list #14 did not visibly remove it from the active tracking audit wait step during the immediate QA window.
- A blocklisted newsletter test contact still appeared in Brevo workflow logs as `Email Newsletter Workflow_step_#2 was sent`; this needs follow-up in delivery logs/inbox to determine whether Brevo logged a send attempt but suppressed delivery, or actually sent.
- CRM Deals settings in the Brevo UI still shows `Set things up first`; pipeline probability cleanup was not accessible through the UI during this QA pass.
- Exact `kenny@alphatrack.digital` did not re-trigger the newsletter workflow because the contact was already on list #9.

## Required follow-up

1. Update/deploy the `backend` branch used by `alphatra-serv` with the CRM fallback and current campaign metadata behavior.
2. Re-run website-to-Brevo-to-CRM QA after the backend deployment.
3. Fix or investigate contact-page validation so the footer newsletter field cannot block the contact form.
4. Add explicit suppression/exit conditions for list #14 where Brevo supports it, or enforce suppression through backend/API rules.
5. Investigate the blocklisted-contact send log before treating blocklist suppression as verified.
6. Complete Brevo CRM Deals setup/settings so pipeline win probabilities can be edited.
